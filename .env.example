# ═══════════════════════════════════════════════
# ClickBoucher — Environment Variables
# Copy to .env and fill in values
# ═══════════════════════════════════════════════

# ── DATABASE ──────────────────────────────────
# PostgreSQL (Railway, Supabase, or local)
DATABASE_URL="postgresql://user:password@localhost:5432/clickboucher?schema=public"

# ── AUTH ──────────────────────────────────────
AUTH_SECRET="change-me-to-a-random-64-char-string"
OTP_EXPIRY_MINUTES=5

# ── PAYMENT ───────────────────────────────────
# "mock" (default) or "stripe"
PAYMENT_PROVIDER="mock"

# Required when PAYMENT_PROVIDER=stripe
# STRIPE_SECRET_KEY="sk_test_..."
# STRIPE_WEBHOOK_SECRET="whsec_..."
# STRIPE_PUBLISHABLE_KEY="pk_test_..."   # (client-side, prefix NEXT_PUBLIC_)
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_..."

# ── NOTIFICATIONS ─────────────────────────────
# "stub" (default) or "twilio"
NOTIFICATION_PROVIDER="stub"

# Required when NOTIFICATION_PROVIDER=twilio
# TWILIO_ACCOUNT_SID="AC..."
# TWILIO_AUTH_TOKEN="..."
# TWILIO_PHONE_NUMBER="+33..."
# TWILIO_WHATSAPP_NUMBER="whatsapp:+33..."

# ── APP CONFIG ────────────────────────────────
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NEXT_PUBLIC_APP_NAME="ClickBoucher"

# Weight tolerance for products sold by weight (%)
WEIGHT_TOLERANCE_PERCENT=10

# How long a last-minute offer reservation is held (minutes)
LAST_MINUTE_HOLD_MINUTES=10

# ── CRON ──────────────────────────────────────
# Secret to authenticate cron endpoint (optional)
# CRON_SECRET="your-cron-secret"

# ── DEPLOYMENT ────────────────────────────────
# NODE_ENV=production
# VERCEL_URL= (auto-set by Vercel)
